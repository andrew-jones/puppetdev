package {'perl-Log-Log4perl':
	ensure => installed
}
package {'perl-Date-Calc':
	ensure => installed
}
package {'lftp':
	ensure => installed
}
package {'perl-Text-CSV_XS':
	ensure => installed
}
package {'perl-DBIx-Class':
	ensure => installed
}
package {'perl-DBIx-Class-Schema-Loader':
	ensure => installed
}
